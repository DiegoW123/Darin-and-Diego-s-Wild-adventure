let randomHero;
// Thought I would start with a variable that will eventually be called to create the hero
let testHero = {
// This Object is a test hero that shows what I want to create randomly
name: 'Hero',
race: 'Half-Elf',
class: 'Monk'
stats: [10, 11, 12, 13, 14, 15, 16, 17, 18]
}
let createRandomHero (function() {
  
}
